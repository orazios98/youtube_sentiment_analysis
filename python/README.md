# Polling with Python Script

The python script extracts the comments from the video specified in the .env file and sends them to Logstash using TCP socket

## Usage

Simply run the docker compose script as explained in the main README.md file. 